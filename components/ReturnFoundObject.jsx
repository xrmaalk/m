import React from "react";

import React from "react";

const ReturnFoundObject = () => {
  return (
    <div className="flex flex-col items-center justify-center min-h-screen">
      ReturnFoundObject
    </div>
  );
};

export default ReturnFoundObject;
